import NavItems from "@/components/layouts/NavItems";

const NavMenu = () => {
    return (
        <nav className="hidden lg:block">
            <NavItems/>
        </nav>
    );
};

export default NavMenu;